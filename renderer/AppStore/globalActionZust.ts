//renderer/AppStore/globalActionZust.ts
import { create } from "zustand";
import { combine } from "zustand/middleware";

export { globalActionZust }

const globalActionZust = create(combine({
    
}, (set, get) => ({
    
})));
